export const enhance = (form: any, submit = () => { }) => {
    return {
        destroy: () => { }
    };
};
export const applyAction = async () => { };
export const deserialize = (str: string) => JSON.parse(str);

---
description: Verify the project builds successfully (Run before asking user to test)
---
1. Navigate to the project root: `cd /home/bobhy/worktrees/dataTable` (or relevant workspace)
2. Run the build command to check for syntax and compilation errors:
   ```bash
   npm run build
   ```
3. If the build fails, fix the errors before proceeding.
// turbo

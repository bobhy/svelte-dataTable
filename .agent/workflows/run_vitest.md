---
description: Run Vitest tests without hanging in watch mode
---
Run Vitest with the `run` command to perform a single test run and exit, preventing the process from hanging in watch mode:

```bash
npx vitest run [optional_file_path]
```
